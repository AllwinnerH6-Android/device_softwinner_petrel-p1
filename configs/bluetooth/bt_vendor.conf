# UART device port where Bluetooth controller is attached
UartPort = /dev/ttyS1

# Firmware patch file location
FwPatchFilePath = /vendor/modules/

# Firmware name
# Do not specify FwPatchFileName = xxx.hcd to enable FwAutoDetection
